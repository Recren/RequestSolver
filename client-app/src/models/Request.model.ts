export interface Request {
  request_id: number,
  title: string,
  assigned_to: string,
  assigned_by: string,
  date_assigned: string,
  date_due: string,
  level_of_severity: string,
  request_description: string
}
